import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "WebP to TIFF Converter — Guide & Free Tools",
  description: "Convert WebP to TIFF for professional print workflows. Learn the best tools since browsers cannot natively encode TIFF.",
  alternates: { canonical: "https://www.toolsz.co/webp-to-tiff" },
};
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
