import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "PNG to HEIC Converter — Guide & Free Tools",
  description: "Convert PNG to HEIC format. HEIC encoding requires Apple devices or special software — learn the best free tools.",
  alternates: { canonical: "https://www.toolsz.co/png-to-heic" },
};
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
