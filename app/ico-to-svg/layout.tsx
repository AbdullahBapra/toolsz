import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "ICO to SVG Converter — Guide & Free Tools",
  description: "Convert ICO favicon to SVG vector format. Browser-based vector conversion isn't supported — learn the best tools and approaches.",
  alternates: { canonical: "https://www.toolsz.co/ico-to-svg" },
};
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
