import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "TIFF to WebP Converter — Guide & Free Tools",
  description: "Convert TIFF to WebP. Browsers can't decode TIFF — learn the best free tools for TIFF to WebP conversion.",
  alternates: { canonical: "https://www.toolsz.co/tiff-to-webp" },
};
export default function Layout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
